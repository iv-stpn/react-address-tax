---
"react-address-tax": minor
---

Expose `COUNTRY_CODES` (and the `CountryCode` type) at the `react-address-tax/codes` subpath so consumers can import the country code reference without pulling in the full component bundle.
