---
"react-address-tax": minor
---

Change `computeTaxOutcome` and `computeConsumerTaxOutcome` to accept a single options object instead of positional arguments, and make `hasNexus` optional (defaults to `false`).
